

block form
  form#form
    .mb-3
      label.form-label(for='title') Titulo
      input#title.form-control(type='text' name='title' value=`${producto.title}`)
    .mb-3
      label.form-label(for='price') Precio
      input#price.form-control(type='number' name='price' min='0' step='0.01' value=`${producto.price}`)
    .mb-3
      label.form-label(for='thumbnail') Imagen
      select#thumbnail.form-select(name='thumbnail' value=`${producto.thumbnail}`)
        if producto.id
          :selectedOption(producto.thumbnail)
        else
          option(selected='') Seleccionar una imagen
          each item in imagenes
            option(value=`${item.path}`, label=`${item.label}`) 
    button.btn.btn-primary(type='submit') Guardar
    if producto.id
      button#eliminar.btn.btn-danger Eliminar producto
  